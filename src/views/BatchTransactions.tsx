import React from "react";

import createBatchTransaction from "../utils/batchTransaction";

const BatchTransactions: React.FC<any> = ({ client, chain }) => {
  return null;
};

export default BatchTransactions;
